{
    "name": "frontend",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        // 🏆 FINAL FIX: After running 'npm run build', copy everything
        // from frontend/dist up to the project root for Vercel deployment.
        "postbuild": "cp -r dist/* ../",
        "serve": "vite preview"
    },
    "dependencies": {
        "solid-js": "^1.9.10"
    },
    "devDependencies": {
        "@types/node": "^24.10.1",
        "typescript": "~5.9.3",
        "vite": "^7.2.4",
        "vite-plugin-solid": "^2.11.10"
    }
}
